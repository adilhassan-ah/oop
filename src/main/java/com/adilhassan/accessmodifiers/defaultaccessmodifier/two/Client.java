package com.adilhassan.accessmodifiers.defaultaccessmodifier.two;

//import com.adilhassan.revision.accessmodifiers.defaultaccessmodifier.one.Student; // This will give a compile-time error

/***
 * 2a. If a class is declared with no access modifier (i.e. default) and belongs to a package then we can access that class
 * from anywhere within the same package only and that's why it is called package-level access
 */
public class Client {
//    Student student; // This will give a compile-time error

    public static void main(String[] args) {
//        Student student1 = new Student(); // This will give a compile-time error
    }
}
